async function importHtmlTemplates() {
    // To Be Implemented
    // -------------------
    // This will need to be run on launch of the reports module.
    // This function will search for any HTML files stored within the ../templates folder and convert them into TSX files, converting the necessary symbols into html unicode, 
    // exporting images as URI, and adding the tsx formatting.
}


export default importHtmlTemplates;